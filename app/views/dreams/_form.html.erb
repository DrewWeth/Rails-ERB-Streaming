<%= form_for(@dream) do |f| %>
  <% if @dream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dream.errors.count, "error") %> prohibited this dream from being saved:</h2>

      <ul>
      <% @dream.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :timeline %><br>
    <%= f.datetime_select :timeline %>
  </div>
  <div class="field">
    <%= f.label :completed %><br>
    <%= f.text_field :completed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
